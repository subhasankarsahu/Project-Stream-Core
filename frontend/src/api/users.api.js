import http, { unwrap } from "./http"
export const usersApi = { channel: async (username) => unwrap(await http.get(`/users/c/${username}`)), history: async () => unwrap(await http.get("/users/history")), changePassword: async (payload) => unwrap(await http.post("/users/change-password", payload)), updateAvatar: async (file) => { const form = new FormData(); form.append("avatar", file); return unwrap(await http.patch("/users/avatar", form)) }, updateCoverImage: async (file) => { const form = new FormData(); form.append("coverImage", file); return unwrap(await http.patch("/users/cover-image", form)) } }
