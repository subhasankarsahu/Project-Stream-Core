import http, { unwrap } from "./http"
export const playlistsApi = { create: async (payload) => unwrap(await http.post("/playlist/", payload)), get: async (playlistId) => unwrap(await http.get(`/playlist/${playlistId}`)), update: async (playlistId, payload) => unwrap(await http.patch(`/playlist/${playlistId}`, payload)), remove: async (playlistId) => unwrap(await http.delete(`/playlist/${playlistId}`)), addVideo: async (videoId, playlistId) => unwrap(await http.patch(`/playlist/add/${videoId}/${playlistId}`)), removeVideo: async (videoId, playlistId) => unwrap(await http.patch(`/playlist/remove/${videoId}/${playlistId}`)), byUser: async (userId) => unwrap(await http.get(`/playlist/user/${userId}`)) }
