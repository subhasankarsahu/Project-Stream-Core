import { Edit3, Heart, MoreVertical, Trash2, UserRound } from "lucide-react"
export function TweetCard({ tweet, currentUserId, onLike, onEdit, onDelete }) { const owner = tweet.owner && typeof tweet.owner === "object" ? tweet.owner : {}; const own = String(tweet.owner?._id || tweet.owner) === String(currentUserId); return <article className="rounded-xl border border-line bg-surface p-5"><div className="flex gap-3"><div className="grid h-10 w-10 shrink-0 place-items-center overflow-hidden rounded-full bg-line text-muted">{owner.avatar ? <img src={owner.avatar} alt="" className="h-full w-full object-cover" /> : <UserRound size={18} />}</div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><span className="font-semibold">@{owner.username || "you"}</span><time className="text-xs text-muted" dateTime={tweet.createdAt}>{new Date(tweet.createdAt).toLocaleDateString()}</time></div><p className="mt-3 whitespace-pre-wrap text-sm leading-6">{tweet.content}</p><button onClick={() => onLike(tweet._id)} className="mt-4 flex items-center gap-2 text-xs text-muted hover:text-white" aria-label="Like tweet"><Heart size={15} /> Like</button></div>{own && <div className="flex gap-1"><button onClick={() => onEdit(tweet)} className="rounded-full p-2 text-muted hover:bg-line hover:text-white" aria-label="Edit tweet"><Edit3 size={15} /></button><button onClick={() => onDelete(tweet)} className="rounded-full p-2 text-muted hover:bg-line hover:text-accent" aria-label="Delete tweet"><Trash2 size={15} /></button></div>}<button className="self-start text-muted hover:text-white" aria-label="Tweet options"><MoreVertical size={17} /></button></div></article> }
