import { Edit3, ListVideo, Trash2 } from "lucide-react"
import { Link } from "react-router-dom"
export function PlaylistCard({ playlist, onEdit, onDelete }) { return <article className="rounded-xl border border-line bg-surface p-4"><Link to={`/playlists/${playlist._id}`} className="block"><div className="grid aspect-video place-items-center rounded-lg bg-line text-muted"><ListVideo size={32} /></div><h2 className="mt-4 truncate font-bold hover:text-accent">{playlist.name}</h2><p className="mt-1 line-clamp-2 text-sm text-muted">{playlist.description}</p><p className="mt-2 text-xs text-muted">{playlist.videos?.length || 0} videos</p></Link><div className="mt-4 flex gap-2 border-t border-line pt-3"><button onClick={() => onEdit(playlist)} className="flex items-center gap-2 rounded-md px-2 py-1 text-xs text-muted hover:bg-line hover:text-white"><Edit3 size={14} /> Edit</button><button onClick={() => onDelete(playlist)} className="flex items-center gap-2 rounded-md px-2 py-1 text-xs text-muted hover:bg-line hover:text-accent"><Trash2 size={14} /> Delete</button></div></article> }
