import { Check, UserRound } from "lucide-react"

export function ChannelHeader({ channel, isOwnProfile, isSubscribed, onSubscribe, isPending }) {
  return <section className="overflow-hidden rounded-xl border border-line bg-surface"><div className="h-36 bg-line md:h-52">{channel.coverImage && <img src={channel.coverImage} alt="" className="h-full w-full object-cover" />}</div><div className="flex flex-col gap-5 p-5 sm:flex-row sm:items-center sm:p-7"><div className="-mt-16 grid h-28 w-28 shrink-0 place-items-center overflow-hidden rounded-full border-4 border-surface bg-line text-muted sm:-mt-20">{channel.avatar ? <img src={channel.avatar} alt={`${channel.username} avatar`} className="h-full w-full object-cover" /> : <UserRound size={34} />}</div><div className="min-w-0 flex-1"><h1 className="text-2xl font-bold">{channel.fullname || channel.fullName || channel.username}</h1><p className="mt-1 text-sm text-muted">@{channel.username}</p><div className="mt-2 flex flex-wrap gap-3 text-sm text-muted"><span>{(channel.subscribersCount || 0).toLocaleString()} subscribers</span><span>{(channel.channelsSubscribedToCount || 0).toLocaleString()} subscriptions</span></div></div>{!isOwnProfile && <button onClick={onSubscribe} disabled={isPending} className={`rounded-full px-5 py-2.5 text-sm font-bold ${isSubscribed ? "bg-line text-white hover:bg-[#444]" : "bg-white text-ink hover:bg-gray-200"}`}>{isSubscribed && <Check className="mr-1 inline" size={15} />}{isSubscribed ? "Subscribed" : "Subscribe"}</button>}</div></section>
}
