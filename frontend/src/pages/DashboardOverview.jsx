import { Activity, Eye, Heart, Plus, Users, Video } from "lucide-react"
import { Link } from "react-router-dom"
import { useDashboardStats, useDashboardVideos } from "../hooks/useDashboardData"
import { StatsCard } from "../components/dashboard/StatsCard"
import { DashboardWidget } from "../components/dashboard/DashboardWidget"
import { UploadedVideosTable } from "../components/dashboard/UploadedVideosTable"

export function DashboardOverview() { const stats = useDashboardStats(); const videos = useDashboardVideos(); const totals = stats.data || {}; const uploaded = videos.data || []; return <section className="space-y-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-widest text-accent">Creator studio</p><h1 className="mt-2 text-3xl font-bold">Channel overview</h1><p className="mt-2 text-muted">A quick read on your channel performance.</p></div><Link to="/studio/upload" className="flex items-center gap-2 rounded-lg bg-accent px-4 py-2.5 text-sm font-bold hover:bg-accent-hover"><Plus size={17} /> Upload video</Link></div>{stats.isError && <p className="text-sm text-accent">Unable to load channel statistics.</p>}<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatsCard label="Total videos" value={totals.totalVideos || 0} icon={Video} accent /><StatsCard label="Total views" value={totals.totalViews || 0} icon={Eye} /><StatsCard label="Subscribers" value={totals.totalSubscribers || 0} icon={Users} /><StatsCard label="Total likes" value={totals.totalLikes || 0} icon={Heart} /></div><div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_280px]"><DashboardWidget title="Uploaded videos" action={<span className="text-xs text-muted">{uploaded.length} total</span>}>{videos.isLoading ? <p className="text-sm text-muted">Loading videos...</p> : videos.isError ? <p className="text-sm text-accent">Unable to load uploaded videos.</p> : <UploadedVideosTable videos={uploaded} />}</DashboardWidget><DashboardWidget title="Channel pulse"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-full bg-line text-muted"><Activity size={18} /></div><div><p className="text-2xl font-bold">{uploaded.filter((video) => video.isPublished).length}</p><p className="text-xs text-muted">published videos</p></div></div><p className="mt-5 text-sm leading-6 text-muted">Keep your titles and thumbnails clear to help viewers decide what to watch next.</p></DashboardWidget></div></section> }
